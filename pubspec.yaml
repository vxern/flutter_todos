publish_to: 'none'

name: flutter_todos
description: A simple to-do app built using Flutter.

version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  go_router: ^12.1.0
  realm: ^1.5.0
  flutter_bloc: ^8.1.3
  sprint: ^3.0.0
  path_provider: ^2.1.1
  universal_io: ^2.2.2
  cryptography_flutter: ^2.3.2
  cryptography: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  words: ^0.4.4

flutter:
  uses-material-design: true

